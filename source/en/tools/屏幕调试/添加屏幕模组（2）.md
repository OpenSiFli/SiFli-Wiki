# Add display module (2) -- Modify the copied driver files

This section does not distinguish between [built-in]((lcd_driver_Internal)) and [external](lcd_driver_Modem). The copied display driver files are modified using the contents of this section.

```{toctree}
:titlesonly:
修改屏幕驱动
修改TP驱动
修改背光驱动
```
